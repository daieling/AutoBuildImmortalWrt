名字：名称：build-x86-64-immortalwrt-24.10.2

在上
  工作流触发输入：
    输入：
      个人资料：
        描述: '请输入要编译固件大小 单位(MB)'
        必填： 真
        默认： '2048'
      包含_docker：
        描述：|
          是否编译 Docker 插件
        必填： 真
        默认：'yes'
        类型：
类型：选择
        选项：
          - 是
          - ‘不’
      启用_pppoe：
        描述: "是否配置PPPoE拨号信息?"
        必填： 真
        默认：'no'
        类型:         类型：选择
        选项:
        - '是'
        - 不
      pppoe账号：
        描述: "宽带账号（若启用PPPoE）"
        必填： 否
      pppoe_password：
        描述: "宽频密码（若启用PPPoE）"
        必填： 否

工作：
  构建:
    运行于：运行于：ubuntu-22.04

    步骤：
      - 名称:       - 名称：结账代码
        uses:         用途：actions/checkout@v3

      - 名称:       - 名称: 设置可执行权限
        run:         运行: chmod +x ${{ github.workspace }}/x86-64/build.sh{{ github.workspace }}/x86-64/build.sh


      - 名称:       - 名称: 验证 PPPoE 输入
        运行： |
          if [[ "${{ inputs.enable_pppoe }}" == "yes" ]]; then
            if [[ -z "${{ inputs.pppoe_account }}" || -z "${{ inputs.pppoe_password }}" ]]; then
              echo "错误：启用PPPoE时必须提供PPPoE账号和密码！"
              退出 1
            输入：fi
          输入：fi

- 名称: - 名称: 构建 ImmortalWrt-x86-64-efi
        运行： |
          profiles="${{ github.event.inputs.profile }}"
          include_docker="${{ github.event.inputs.include_docker }}"
IFS=',' read -r -a profile_array << $profiles
          
          对于“${profile_array[@]}”中的每个配置文件
            echo "正在为配置文件 $profile 编译"
            
            docker run --rm -i \
              --用户 root
              -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
              -v "${{ github.workspace }}/files:/home/build/immortalwrt/files" \
              -v "${{ github.workspace }}/x86-64/imm.config:/home/build/immortalwrt/.config" \
              -v "${{ github.workspace }}/shell:/home/build/immortalwrt/shell" \
              -v "${{ github.workspace }}/x86-64/24.10/build.sh:/home/build/immortalwrt/build.sh" \
              -e PROFILE=$profile \
              -e INCLUDE_DOCKER=$include_docker \
              -e ENABLE_PPPOE=${{ inputs.enable_pppoe }} \
              -e PPPOE_ACCOUNT=${{ inputs.pppoe_account }} \
              -e PPPOE_PASSWORD=${{ inputs.pppoe_password }} \
              immortalwrt/imagebuilder:x86-64-openwrt-24.10.2 /bin/bash /home/build/immortalwrt/build.sh
          done

      - name: Generate Firmware SHA-256
        run: |
          cp ${{ github.workspace }}/bin/targets/x86/64/*squashfs-combined-efi.img.gz ${{ github.workspace }}
          for file in *squashfs-combined-efi.img.gz; do
            sha256sum "$file" > "$file.sha256"
            sha256sum -c "$file.sha256"
          done

      - name: Create info
        run: |
          if [ "${{ github.event.inputs.include_docker }}" == "yes" ]; then
            extra_content="#### 默认带docker"
            echo -e "\n $extra_content" >> ${{ github.workspace }}/info.md
          else
            echo -e "NO docker"
          fi

      - name: Upload ImmortWrt as release assets
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: Autobuild-x86-64
          body_path: ${{ github.workspace }}/info.md
          files: |
            ${{ github.workspace }}/*squashfs-combined-efi.img.gz
            ${{ github.workspace }}/*.sha256
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
